# Firebase Setup
1. Create Firebase project `digital-detox-coach`.
2. Register Android app `com.digitaldetox.coach`.
3. Download `google-services.json` into `android/app/`.
4. Enable Analytics and Crashlytics.
5. Replace placeholder values in `lib/firebase_options.dart`.
