# AdMob Setup
1. Create AdMob app for Android package `com.digitaldetox.coach`.
2. Replace application ID in AndroidManifest metadata.
3. Replace banner ad unit id in `AppConstants` for release builds.
4. Keep Google test IDs in debug builds.
5. Implement UMP SDK consent flow before personalized ads in production.
